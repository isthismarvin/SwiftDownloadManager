import Foundation

enum AppConstants {
    static let chromeExtensionVersion = "2.1.1"
    static var chromeExtensionVersionLabel: String { "v\(chromeExtensionVersion)" }
    static let urlScheme = "swiftdownloadmanager"
}
