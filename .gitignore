# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
.derivedData/
*.xcuserstate
*.xcscmblueprint

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Build outputs
build/
dist/
*.o
*.a
*.dylib
*.dSYM/
tests/unittests
smoke/enginetest

# Machine-local editor / LSP (optional — remove if you commit shared settings)
buildServer.json

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
# macOS custom folder icon (Icon\r) — avoid Icon? which also matches */icons/
Icon

# CocoaPods (if added later)
Pods/

# Fastlane (if added later)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Instruments
*.trace

# SwiftLint / SwiftFormat
.swiftlint-cache/

# Editor / IDE
.cursor/
.vscode/settings.json
*.swp
*.swo

# Xcode build metadata (regenerated locally)
*.xcbuilddata

# Secrets — never commit credentials
.env
.env.*
*.pem
*.p12
secrets.json
